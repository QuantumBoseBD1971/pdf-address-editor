{
  "name": "pdf-address-editor",
  "version": "1.0.0",
  "main": "electron/main.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "electron": "electron .",
    "dist": "vite build && electron-builder --publish never"
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "pdfjs-dist": "^4.0.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "electron": "^31.0.0",
    "electron-builder": "^24.0.0",
    "@vitejs/plugin-react": "^5.0.0"
  }
}
